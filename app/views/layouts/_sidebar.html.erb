<div id="sidebar">
<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="panel-title">Things to do...</h3>

  </div>
  <div class="panel-body">
    <p>
      <%= link_to ' All Books', books_path, :class => 'btn btn-default btn-block'  %>
    </p>
    <p>
      <%= link_to ' Authors', authors_path, :class => 'btn btn-default btn-block' %>
    </p>

    <p>
      <%= link_to 'My Reservations', reservations_path, :class => 'btn btn-default btn-block' %>
    </p>
    <!-- comment Use the session[:admin] variable from browser -->
    <p>
      <% if session[:admin] == true %>
      <%= link_to 'New Book', new_book_path, :class => 'btn btn-default btn-block' %>
      <% end %>
    </p>
    <p>
      <% if session[:admin] == true %>
      <%= link_to 'Overdue Books', reservations_overdue_path, :class => 'btn btn-default btn-block' %>
      <% end %>
    </p>

    <p>
      <% if session[:admin] == true %>
      <%= link_to 'Create Author', new_author_path, :class => 'btn btn-default btn-block' %>
      <% end %>
    </p>

    <p>
    <% if session[:admin] == true %>
    <%= link_to 'Manage Users', users_path, :class => 'btn btn-default btn-block' %>
    <% end %>
    </p>
    <p>
      <%= link_to 'Logout', sessions_destroy_path, :class => 'btn btn-default btn-block' %>
    </p>
  </div>
</div>
</div>